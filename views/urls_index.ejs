<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include ./partials/_header %>
</header>
<table>
    <% for(shortURL in urls) { %>
      <tr>
    <td><%= shortURL %></td>
    <td><%= urls[shortURL] %></td>
    <td><a href="http://localhost:8080/urls/<%= shortURL %>"">Edit</a></td>
    <td>
      <form method="POST" action= "/urls/<%= shortURL %>/delete">
      <input type="submit" value="delete" name=""/>
    </form>
  </td>

</tr>
  <% } %>
</table>
</body>
</html>